{
  "description": "Smoke fixture for the bundled translate package: proves the recipe wiring (llm_complete + pick + format) executes without throwing. The emulator's own canned reply, not a real translation - lib/smoke.ts's recipe_fixture kind runs against a HostEmulator, never a real chat model (spec/emulators/ts/host-emulator.ts).",
  "inputs": { "expression": "hello world to spanish" },
  "host_setup": {},
  "expected": {
    "reply": {
      "text": "[emulator: no model loaded, this is a canned reply]",
      "speech": "[emulator: no model loaded, this is a canned reply]"
    },
    "actions": [],
    "scheduled_jobs": [],
    "home_calls": [],
    "memory_added": []
  }
}
