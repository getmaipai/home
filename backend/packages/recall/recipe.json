{
  "id": "recall",
  "inputs": [{ "name": "topic", "selector": "text" }],
  "steps": [
    {
      "op": "recall",
      "as": "summary",
      "query": "{topic}"
    },
    {
      "op": "format",
      "as": "reply",
      "text": "{summary}",
      "speech": "{summary}"
    }
  ]
}
