import ModernDashboard from "@maipai/ui/src/dashboard/views/dashboards/modern";

/** /next: the stand-up's dashboard route (docs/plans/
 * shell-on-shadcndashboard-2026-09-21.md, step 1) - the template's own
 * modern dashboard view, on its own demo data. */
export function NextDashboardPage() {
  return <ModernDashboard />;
}
