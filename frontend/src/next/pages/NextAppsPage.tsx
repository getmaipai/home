import TablesPage from "@maipai/ui/src/dashboard/views/pages/tables";

/** /next/apps: the stand-up's data-tables route (docs/plans/
 * shell-on-shadcndashboard-2026-09-21.md, step 1) - the template's own
 * data-tables view, on its own demo data. */
export function NextAppsPage() {
  return <TablesPage />;
}
