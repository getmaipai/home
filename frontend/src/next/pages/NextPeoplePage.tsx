import UserProfilePage from "@maipai/ui/src/dashboard/views/pages/user-profile";

/** /next/people: the stand-up's people route (docs/plans/
 * shell-on-shadcndashboard-2026-09-21.md, step 1) - the template's own
 * user-profile view, on its own demo data. */
export function NextPeoplePage() {
  return <UserProfilePage />;
}
