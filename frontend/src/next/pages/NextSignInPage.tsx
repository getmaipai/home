import BoxedLogin from "@maipai/ui/src/dashboard/views/auth/auth2/login";

/** /next/sign-in: the stand-up's auth route (docs/plans/
 * shell-on-shadcndashboard-2026-09-21.md, step 1) - the template's own
 * auth view, on its own demo data. A preview only: signing in here
 * doesn't do anything yet, the same as every other /next route. */
export function NextSignInPage() {
  return <BoxedLogin />;
}
