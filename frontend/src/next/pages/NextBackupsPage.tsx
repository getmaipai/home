import TablesPage from "@maipai/ui/src/dashboard/views/pages/tables";

/** /next/backups: the stand-up's data-tables route for the Manage group (step 1); real data is a step-2 row. */
export function NextBackupsPage() {
  return <TablesPage />;
}
