{
  "description": "compute + format: a hex literal survives the spoken-arithmetic normalization (#107).",
  "recipe": {
    "id": "compute-hex-literal",
    "inputs": [{ "name": "expression", "selector": "text" }],
    "steps": [
      { "op": "compute", "as": "result", "expression": "{expression}" },
      { "op": "format", "as": "reply", "text": "{result}", "speech": "{result}" }
    ]
  },
  "inputs": { "expression": "0x10" },
  "host_setup": {},
  "expected": {
    "reply": { "text": "16", "speech": "16" },
    "actions": [],
    "scheduled_jobs": [],
    "home_calls": [],
    "memory_added": []
  }
}
