# generated by datamodel-codegen:
#   filename:  result.schema.json

from __future__ import annotations

from typing import Any

from pydantic import BaseModel, ConfigDict, Field


class Reply(BaseModel):
    model_config = ConfigDict(
        extra='forbid',
    )
    text: str
    speech: str | None = None


class Confirm(BaseModel):
    model_config = ConfigDict(
        extra='forbid',
    )
    prompt: str | None = None
    on_confirm: dict[str, Any] | None = None


class Ask(BaseModel):
    """
    Lets a deterministic follow-up match without a model (4.5).
    """

    model_config = ConfigDict(
        extra='forbid',
    )
    prompt: str | None = None
    expects: str | None = None


class SkillResult(BaseModel):
    """
    What a package's handle() (or a recipe's interpreted run) returns. See platform plan 4.9.
    """

    model_config = ConfigDict(
        extra='forbid',
    )
    reply: Reply | None = None
    data: Any | None = None
    synthesis_hint: str | None = None
    actions: list[dict[str, Any]] | None = []
    directive: dict[str, Any] | None = None
    confirm: Confirm | None = None
    ask: Ask | None = Field(
        None, description='Lets a deterministic follow-up match without a model (4.5).'
    )
    end_conversation: bool | None = None
    article: dict[str, Any] | None = None
