# generated by datamodel-codegen:
#   filename:  settings-key.schema.json

from __future__ import annotations

from typing import Any, Literal

from pydantic import BaseModel, ConfigDict, Field, constr


class Section(BaseModel):
    model_config = ConfigDict(
        extra='forbid',
    )
    id: str | None = None
    collapsed: bool | None = False
    order: int | None = None


class SettingsKey(BaseModel):
    """
    One entry in the settings registry (spec/settings/keys.json) or a package manifest's config[]. See platform plan 3.2 and .github's docs/SETTINGS.md.
    """

    model_config = ConfigDict(
        extra='forbid',
    )
    key: constr(pattern=r'^[a-z][a-z0-9_]*(\.[a-z][a-z0-9_]*)+$')
    scope: Literal['household', 'person', 'device']
    selector: Literal[
        'number',
        'select',
        'text',
        'boolean',
        'duration',
        'time',
        'entity',
        'area',
        'person',
        'media',
    ] = Field(..., description="Home Assistant's selector names (3.2).")
    range: Any | None = Field(
        None,
        description='Selector-specific: min/max for number, a duration unit, an option list for select, and so on. Shape depends on selector.',
    )
    default: Any
    label: constr(min_length=1)
    help: str | None = None
    section: Section | None = None
    level: Literal['basic', 'advanced', 'expert']
    secret: bool | None = False
    needs: list[str] | None = Field(
        None, description='Capabilities required for this key to apply.'
    )
    lives_in: str = Field(
        ...,
        description='The package, companion, integration, or central page id that renders this key.',
    )
    honoured_by: list[Literal['home', 'bot']] = Field(..., min_length=1)
