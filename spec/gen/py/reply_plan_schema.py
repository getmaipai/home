# generated by datamodel-codegen:
#   filename:  reply-plan.schema.json

from __future__ import annotations

from typing import Literal

from pydantic import BaseModel, ConfigDict, Field, conint


class Moves(BaseModel):
    model_config = ConfigDict(
        extra='forbid',
    )
    react: Literal['required', 'allowed', 'forbidden'] = Field(
        ..., description='A short reaction to the emotion or the news.'
    )
    care: Literal['required', 'allowed', 'forbidden'] = Field(
        ...,
        description='An acknowledgment of a marked emotion, ahead of any other content.',
    )
    say: Literal['required', 'allowed', 'forbidden'] = Field(
        ..., description='The substantive answer or acknowledgment.'
    )
    pick: Literal['required', 'allowed', 'forbidden'] = Field(
        ..., description='Choosing one of several candidates the evidence offers.'
    )
    point: Literal['required', 'allowed', 'forbidden'] = Field(
        ...,
        description='Citing a source, a link, or a document - forbidden on the child band (section 13 part 1).',
    )
    ask_back: Literal['required', 'allowed', 'forbidden'] = Field(
        ..., description='A follow-up question.'
    )
    close: Literal['required', 'allowed', 'forbidden'] = Field(
        ..., description='A reciprocal closing.'
    )
    defer: Literal['required', 'allowed', 'forbidden'] = Field(
        ...,
        description='AGE-01 (section 13 part 3): naming a trusted adult as the place for what the hub will not decide or recite, rather than pointing to a source. The trusted-adult move, distinct from `point`.',
    )


class ReplyPlan(BaseModel):
    """
    SPEC-01 (dev.md section 12 part 3, amended by section 13 part 9 and the coherence review, question 1). A plan of permitted moves, not an order: the engine turns the signal, the protocol state, the surface, an explicit brevity request, the age band and the available evidence into this, and the composer realizes it in the companion's voice. Replaces the composer's earlier fixed 'react, pick, say, point' order (dev.md section 6 part 1, amended). A value shape, carried on conversation-turn.schema.json's own `plan` field, never synced independently. The move vocabulary is declared once, exactly here: react, care, say, pick, point, ask_back, close, defer - no item may add a ninth.

    What this deliberately does not carry (ACT-03 narrowed, the coherence review's over-design finding): no `claim_state` (section 14, deferred to CRED-01), no `credulity` block (section 15, deferred), no typed move fields on a streamed chat turn (those exist only on a composed/lookup turn's ComposedTurn, off the interactive path per the review's cost analysis - a streamed reply is checked against this plan at the guard boundary instead, by deterministic signs in the sentences).
    """

    model_config = ConfigDict(
        extra='forbid',
    )
    moves: Moves
    playfulness: Literal['allowed', 'forbidden'] = Field(
        ...,
        description='Forbidden under every negative-emotion override and on the child band whenever a negative emotion or a harm/illness/death/living-thing question is in play (section 13 part 1).',
    )
    max_sentences: conint(ge=1)
    max_words: conint(ge=1)
    age_band: Literal['child', 'teen', 'adult'] = Field(
        ...,
        description='Copied from the frozen TurnSignal.age_band, so the plan is self-contained for the composer and the guards without a second read of the signal.',
    )
    vocabulary_level: Literal['simple', 'standard', 'advanced'] = Field(
        ...,
        description="Section 13 part 1's own `complexity` dial, renamed and moved here (the coherence review, question 1: 'vocabulary_level replaces a forced complexity dial, one field'). `simple` forced on the child band; capped at `standard` on the teen band; the companion's own dial on adult, never above what the band allows.",
    )
    explanation_style: Literal['concrete', 'plain', 'full'] = Field(
        ...,
        description="Section 13 part 1: `concrete` on the child band (concrete words, no abstractions, no idioms a child cannot parse); `plain` on the teen band (no talking down); `full` on the adult band (the companion's own register). AGE-01/ACT-03's own field to build against; this shape is a starting point, not yet read by any engine code.",
    )
    trusted_adult_move: Literal['none', 'name_adult', 'offer_to_ask'] = Field(
        ...,
        description="Section 13 part 3: how the `defer` move, when present, should read. `name_adult`: name a trusted adult as the place for the answer. `offer_to_ask`: also offer to relay the child's question to them (queues an OpenQuestion of kind `relay`). `none` when `defer` is not `required` or `allowed` this turn. AGE-01/ACT-03's own field to build against; this shape is a starting point, not yet read by any engine code.",
    )
    content_disclosure: Literal['full', 'some_withheld'] = Field(
        ...,
        description="Section 13 part 2: whether every piece of evidence handed to the composer this turn was shown in full, or at least one item was summarized or withheld by the content ceiling or a household record's child_disclosure - so the reply can say so honestly ('some of what came back is for grown-ups') rather than pretending nothing was found. The per-item detail lives on the bench's own evidenceDisposition expectation, not here; this is the one summary bit the plan carries. AGE-01/ACT-03's own field to build against; this shape is a starting point, not yet read by any engine code.",
    )
