# generated by datamodel-codegen:
#   filename:  reply-constraint.schema.json

from __future__ import annotations

from typing import Literal

from pydantic import AwareDatetime, BaseModel, ConfigDict, Field, constr


class ReplyConstraint(BaseModel):
    """
    CONS-01, dev.md section 16 part 9 rule 2: a live constraint the turn engine sets on the hub's next reply in a given conversation. kind banned_phrase: the hub must never say the value's phrase. kind shape: the reply must take the value's shape token. kind length: the reply must stay within the value's positive-integer character budget, carried as a string.
    """

    model_config = ConfigDict(
        extra='forbid',
    )
    id: constr(pattern=r'^rc-[a-z0-9]{6,}$')
    conversation_id: constr(pattern=r'^conv-[a-z0-9]{6,}$') = Field(
        ...,
        description='The conversation this constraint applies to, for the duration of it. Required: a reply constraint with no conversation to constrain is meaningless.',
    )
    person: constr(pattern=r'^person-[a-z0-9]{6,}$') | None = Field(
        ...,
        description='Who the constraint was set for, when there is a person the reply is about - null when the hub set it for the conversation itself.',
    )
    kind: Literal['banned_phrase', 'shape', 'length'] = Field(
        ...,
        description="banned_phrase: the hub must never say the value's phrase. shape: the reply must take the value's shape token (list, number, one_line). length: the reply must stay within the value's character budget.",
    )
    value: constr(min_length=1) = Field(
        ...,
        description="The constraint's own value, shaped by kind: the banned phrase itself, a shape token, or a positive-integer character budget as a string.",
    )
    set_at: AwareDatetime
    set_by_turn: constr(pattern=r'^turn-[a-z0-9]{6,}$') | None = Field(
        ...,
        description="The turn that set this constraint, matching open-question.schema.json's provenance convention. Null when the constraint was set outside any recorded turn.",
    )
    hlc: constr(pattern=r'^[0-9]+:[0-9]+:[a-z0-9]{6,}$') = Field(
        ...,
        description='Hybrid logical clock: wall_ms:counter:node (7.3), the same shape every other synced record type uses.',
    )
