# generated by datamodel-codegen:
#   filename:  source.schema.json

from __future__ import annotations

from typing import Literal

from pydantic import AnyUrl, AwareDatetime, BaseModel, ConfigDict, Field, constr


class Source(BaseModel):
    """
    One citation on an assistant reply: the evidence ladder CHAT-16 phrases factual answers from (media-conversation-program-2026-09-13.md step 4 - a typed source, then websearch, then model knowledge), numbered and rendered as `[N]` markers in the reply text (docs/BACKLOG.md's 'Inline citation markers on sourced answers', the Perplexity-shaped pattern legacy already validated once). Lives on the assistant turn (backend/src/wire.ts's TurnValue, additive when CHAT-16 lands - home is the whole record, no separate hub/robot split for this one), never independently synced or merged, which is why it carries the shared envelope (source, hlc, created_at) but no updated_at: a citation is a snapshot of what was true when the reply was generated, not an editable record.
    """

    model_config = ConfigDict(
        extra='forbid',
    )
    id: constr(pattern=r'^src-[a-z0-9]{6,}$')
    kind: Literal['web', 'wikidata', 'wikipedia', 'weather', 'package'] = Field(
        ...,
        description="Which evidence rung this came from: web is the household's own SearXNG; wikidata/wikipedia/weather are the typed lookups CHAT-15 already retains; package is a future catalog package's own citation (its manifest names the site).",
    )
    title: constr(min_length=1) = Field(
        ...,
        description="The cited page or result's own title, exactly as the source gave it, never rewritten by the model.",
    )
    url: AnyUrl = Field(
        ...,
        description='Opened directly by the citation chip, rel="noopener noreferrer" and referrerpolicy="no-referrer" on the client (the privacy page\'s promise: a cited site learns nothing from the click but the click).',
    )
    site: constr(min_length=1) = Field(
        ...,
        description='The hostname a citation chip shows (e.g. "wikipedia.org"), so a household member sees where an answer came from without hovering the link.',
    )
    snippet: str | None = Field(
        None,
        description='The short passage the evidence was drawn from, when the source has one worth showing; null for a source that is just a link (e.g. a typed weather lookup).',
    )
    source: constr(min_length=1) = Field(
        ...,
        description="Free-text provenance: the turn id this citation was gathered for (mirrors memory-record.schema.json's own 'source' field and naming).",
    )
    created_at: AwareDatetime
    hlc: constr(pattern=r'^[0-9]+:[0-9]+:[a-z0-9]{6,}$') = Field(
        ..., description='Hybrid logical clock: wall_ms:counter:node (7.3).'
    )
