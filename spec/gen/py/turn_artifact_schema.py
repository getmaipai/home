# generated by datamodel-codegen:
#   filename:  turn-artifact.schema.json

from __future__ import annotations

from typing import Literal

from pydantic import AwareDatetime, BaseModel, ConfigDict, Field, conint, constr

from . import source_schema


class ArtifactSubject(BaseModel):
    model_config = ConfigDict(
        extra='forbid',
    )
    id: constr(pattern=r'^subject-[a-z0-9]{6,}$')
    name: constr(min_length=1)


class ArtifactValue(BaseModel):
    model_config = ConfigDict(
        extra='forbid',
    )
    subject_id: constr(pattern=r'^subject-[a-z0-9]{6,}$')
    value: constr(min_length=1)


class ArtifactRow(BaseModel):
    model_config = ConfigDict(
        extra='forbid',
    )
    attribute: constr(min_length=1)
    values: list[ArtifactValue] = Field(..., min_length=2)


class Result(BaseModel):
    model_config = ConfigDict(
        extra='forbid',
    )
    title: constr(min_length=1)
    line: constr(min_length=1) = Field(
        ...,
        description='One source-backed line for the result, never a model-authored paragraph.',
    )
    source_id: constr(pattern=r'^src-[a-z0-9]{6,}$') = Field(
        ...,
        description="The id of a citation in the document's top-level sources array.",
    )


class Lookup(BaseModel):
    model_config = ConfigDict(
        extra='forbid',
    )
    type: Literal['lookup']
    query: constr(min_length=1) = Field(
        ..., description='The bounded lookup expression that produced the result list.'
    )
    results: list[Result] = Field(..., min_length=1)


class FilmCard(BaseModel):
    model_config = ConfigDict(
        extra='forbid',
    )
    type: Literal['card']
    kind: Literal['film']
    name: constr(min_length=1)
    year: conint(ge=1888) | None
    director: constr(min_length=1) | None
    genres: list[constr(min_length=1)]
    source_id: constr(pattern=r'^src-[a-z0-9]{6,}$') = Field(
        ...,
        description="The id of a citation in the document's top-level sources array.",
    )


class PersonCard(BaseModel):
    model_config = ConfigDict(
        extra='forbid',
    )
    type: Literal['card']
    kind: Literal['person']
    name: constr(min_length=1)
    occupation: constr(min_length=1) | None
    known_for: list[constr(min_length=1)]
    source_id: constr(pattern=r'^src-[a-z0-9]{6,}$') = Field(
        ...,
        description="The id of a citation in the document's top-level sources array.",
    )


class PlaceCard(BaseModel):
    model_config = ConfigDict(
        extra='forbid',
    )
    type: Literal['card']
    kind: Literal['place']
    name: constr(min_length=1)
    region: constr(min_length=1) | None
    country: constr(min_length=1) | None
    source_id: constr(pattern=r'^src-[a-z0-9]{6,}$') = Field(
        ...,
        description="The id of a citation in the document's top-level sources array.",
    )


class Quantity(BaseModel):
    model_config = ConfigDict(
        extra='forbid',
    )
    amount: float | constr(min_length=1)
    unit: constr(min_length=1)
    item: constr(min_length=1)


class Step(BaseModel):
    model_config = ConfigDict(
        extra='forbid',
    )
    position: conint(ge=1)
    instruction: constr(min_length=1)
    quantities: list[Quantity]


class Procedure(BaseModel):
    model_config = ConfigDict(
        extra='forbid',
    )
    type: Literal['procedure']
    title: constr(min_length=1)
    steps: list[Step] = Field(..., min_length=1)


class Comparison(BaseModel):
    model_config = ConfigDict(
        extra='forbid',
    )
    type: Literal['comparison']
    title: constr(min_length=1)
    subjects: list[ArtifactSubject] = Field(..., min_length=2)
    rows: list[ArtifactRow] = Field(..., min_length=1)


class TurnArtifact(BaseModel):
    """
    COMP-01's immutable details document for one assistant turn. The section is built from retained typed outcomes, not model prose. A changed evidence version creates a new revision; an existing revision is never edited in place.
    """

    model_config = ConfigDict(
        extra='forbid',
    )
    id: constr(pattern=r'^doc-[a-z0-9]{6,}$') = Field(
        ...,
        description='Stable identity for this document revision. A later revision has a new id.',
    )
    turn_id: constr(pattern=r'^turn-[a-z0-9]{6,}$') = Field(
        ...,
        description='The assistant turn whose retained outcomes produced this document.',
    )
    revision: conint(ge=1) = Field(
        ...,
        description='One-based revision number for a living document on the same subject.',
    )
    evidence_version: constr(min_length=1) = Field(
        ...,
        description='Stable version of the retained outcome evidence used to build this revision.',
    )
    section: Lookup | Procedure | Comparison | FilmCard | PersonCard | PlaceCard
    sources: list[source_schema.Source] = Field(
        ...,
        description='The citation snapshots available to this document. Child-band delivery strips this list without widening the content ceiling.',
        min_length=1,
    )
    provenance: constr(min_length=1) = Field(
        ...,
        description='Free-text provenance for the build, such as the composer run or the originating outcome.',
    )
    created_at: AwareDatetime
    hlc: constr(pattern=r'^[0-9]+:[0-9]+:[a-z0-9]{6,}$') = Field(
        ..., description='Hybrid logical clock: wall_ms:counter:node (7.3).'
    )
