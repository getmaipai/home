# generated by datamodel-codegen:
#   filename:  error-entry.schema.json

from __future__ import annotations

from pydantic import BaseModel, ConfigDict, Field, constr


class ErrorEntry(BaseModel):
    """
    One code in the shared error catalogue (spec/errors/errors.json). Every package error the host wraps maps to one of these. See platform plan 4.9 and docs/ENGINEERING.md > Errors.
    """

    model_config = ConfigDict(
        extra='forbid',
    )
    code: constr(pattern=r'^[a-z][a-z0-9_]*$')
    message: str = Field(
        ..., description='Developer-facing, goes in logs and diagnostics.'
    )
    spoken_fallback: str = Field(
        ...,
        description='What the robot or a voice surface says when this error surfaces mid-conversation.',
    )
    ui_message: str = Field(
        ...,
        description='What the shell shows: specific title, what happened, what to do (docs/UI.md pattern table > Errors).',
    )
    retriable: bool
