# generated by datamodel-codegen:
#   filename:  reply-feedback.schema.json

from __future__ import annotations

from typing import Literal

from pydantic import AwareDatetime, BaseModel, ConfigDict, Field, constr


class ReplyFeedback(BaseModel):
    """
    FEED-01: one person's rating of one assistant reply, kept as a human label for the review program. This record joins the conversation turn by turn_id and is per person, so a rating is never a hidden aggregate and never changes the reply it describes. The reason is a one-tap optional label for a down rating; the engine and classifier remain the only writers of reply content.
    """

    model_config = ConfigDict(
        extra='forbid',
    )
    id: constr(pattern=r'^rf-[a-z0-9]{6,}$') = Field(
        ...,
        description="The feedback record's stable id. A later change to this person's rating updates this record rather than creating a second label.",
    )
    turn_id: constr(pattern=r'^turn-[a-z0-9]{6,}$') = Field(
        ...,
        description="The assistant turn being rated, and the join key into RVW-1's weekly label export.",
    )
    person_id: constr(pattern=r'^person-[a-z0-9]{6,}$') = Field(
        ...,
        description="The household person who supplied the rating. Feedback is private to this actor's label, not a household-wide counter.",
    )
    verdict: Literal['up', 'down'] = Field(
        ..., description="The person's one-tap verdict on the reply."
    )
    reason: Literal['wrong', 'too_long', 'did_not_listen', 'off', 'unsafe'] | None = (
        Field(
            None,
            description="Optional one-tap explanation, selected from FEED-01's fixed five-value list. The child band never exposes or stores a reason.",
        )
    )
    source: constr(min_length=1) = Field(
        ...,
        description='Free-text provenance for the write path, matching the source field on sibling records.',
    )
    created_at: AwareDatetime = Field(..., description='When this label was written.')
    hlc: constr(pattern=r'^[0-9]+:[0-9]+:[a-z0-9]{6,}$') = Field(
        ...,
        description='Hybrid logical clock: wall_ms:counter:node (7.3), the clock stamp used by every synced record shape.',
    )
