{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://getmaipai.github.io/home/spec/schemas/result.schema.json",
  "title": "PluginResult",
  "description": "What a package's handle() (or a recipe's interpreted run) returns. See platform plan 4.9.",
  "type": "object",
  "additionalProperties": false,
  "properties": {
    "reply": {
      "type": "object",
      "additionalProperties": false,
      "required": ["text"],
      "properties": {
        "text": { "type": "string" },
        "speech": { "type": "string" }
      }
    },
    "data": {},
    "synthesis_hint": { "type": "string" },
    "actions": {
      "type": "array",
      "items": { "type": "object", "additionalProperties": true },
      "default": []
    },
    "directive": { "type": "object", "additionalProperties": true },
    "confirm": {
      "type": "object",
      "additionalProperties": false,
      "properties": {
        "prompt": { "type": "string" },
        "on_confirm": { "type": "object", "additionalProperties": true }
      }
    },
    "ask": {
      "type": "object",
      "additionalProperties": false,
      "description": "Lets a deterministic follow-up match without a model (4.5).",
      "properties": {
        "prompt": { "type": "string" },
        "expects": { "type": "string" }
      }
    },
    "end_conversation": { "type": "boolean" },
    "article": { "type": "object", "additionalProperties": true },
    "error": {
      "type": "object",
      "description": "Fix B (docs/dev.md's 'Chat reliability: the 2026-09-07 incident' note): a handler's own typed report that it could not answer (an upstream fetch failure, say) - never present alongside `reply`. Distinct from a thrown exception (which the Tier 1 host maps to a strike and the manifest's fallback_reply already): this is a report of an EXPECTED failure mode a handler catches itself, so the caller can answer with the household-facing fallback without treating the package's own sandbox as unhealthy.",
      "additionalProperties": false,
      "required": ["code", "message"],
      "properties": {
        "code": { "type": "string", "description": "One of spec/errors/errors.json's own codes." },
        "message": { "type": "string", "description": "Developer-facing detail, never shown to the household - the household sees the manifest's own fallback_reply or the error catalogue's spoken_fallback instead." }
      }
    }
  }
}
