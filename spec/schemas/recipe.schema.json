{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://getmaipai.github.io/home/spec/schemas/recipe.schema.json",
  "title": "Recipe",
  "description": "A Tier 0 declarative package body, interpreted natively by the TS and Python interpreters in spec/interpreters/. See platform plan 5.2. A recipe is a named set of inputs plus a list of steps; each step is one of the primitives below.",
  "type": "object",
  "additionalProperties": false,
  "required": ["id", "steps"],
  "properties": {
    "id": { "type": "string", "minLength": 1 },
    "inputs": {
      "type": "array",
      "description": "Selectors a family fills in to instantiate this recipe (e.g. 'bedtime reminder for {person}').",
      "items": {
        "type": "object",
        "additionalProperties": false,
        "required": ["name", "selector"],
        "properties": {
          "name": { "type": "string", "pattern": "^[a-z][a-z0-9_]*$" },
          "selector": {
            "type": "string",
            "enum": [
              "number", "select", "text", "boolean", "duration", "time",
              "entity", "area", "person", "media"
            ]
          },
          "required": { "type": "boolean", "default": true }
        }
      }
    },
    "steps": {
      "type": "array",
      "minItems": 1,
      "items": { "$ref": "#/$defs/step" }
    }
  },
  "$defs": {
    "step": {
      "oneOf": [
        { "$ref": "#/$defs/fetch_step" },
        { "$ref": "#/$defs/pick_step" },
        { "$ref": "#/$defs/format_step" },
        { "$ref": "#/$defs/home_call_service_step" },
        { "$ref": "#/$defs/action_step" },
        { "$ref": "#/$defs/remember_step" },
        { "$ref": "#/$defs/recall_step" },
        { "$ref": "#/$defs/schedule_step" },
        { "$ref": "#/$defs/integration_call_step" },
        { "$ref": "#/$defs/compute_step" },
        { "$ref": "#/$defs/llm_complete_step" },
        { "$ref": "#/$defs/list_add_step" },
        { "$ref": "#/$defs/list_view_step" },
        { "$ref": "#/$defs/remind_step" },
        { "$ref": "#/$defs/timer_step" },
        { "$ref": "#/$defs/ask_step" }
      ]
    },
    "fetch_step": {
      "type": "object",
      "additionalProperties": false,
      "required": ["op", "as", "url"],
      "properties": {
        "op": { "const": "fetch" },
        "as": { "type": "string", "description": "Binds the result into the recipe's variable scope under this name." },
        "url": { "type": "string", "description": "May reference input/variable names in {braces}." },
        "method": { "type": "string", "enum": ["GET", "POST"], "default": "GET" },
        "headers": { "type": "object", "additionalProperties": { "type": "string" } },
        "body": {}
      },
      "description": "Goes through host.fetch (the rate limiter's single choke point). A recipe never bypasses it."
    },
    "pick_step": {
      "type": "object",
      "additionalProperties": false,
      "required": ["op", "as", "from"],
      "properties": {
        "op": { "const": "pick" },
        "as": { "type": "string" },
        "from": { "type": "string", "description": "A variable name to read from." },
        "path": { "type": "string", "description": "A dotted path into that variable, e.g. results.0.title." }
      }
    },
    "format_step": {
      "type": "object",
      "additionalProperties": false,
      "required": ["op", "as", "text"],
      "properties": {
        "op": { "const": "format" },
        "as": { "type": "string" },
        "text": { "type": "string", "description": "A template with {variable} interpolation for the on-screen reply." },
        "speech": { "type": "string", "description": "A template for the spoken form. Falls back to text if omitted." }
      }
    },
    "home_call_service_step": {
      "type": "object",
      "additionalProperties": false,
      "required": ["op", "domain", "service", "target"],
      "properties": {
        "op": { "const": "home.call_service" },
        "domain": { "type": "string" },
        "service": { "type": "string" },
        "target": { "type": "object", "additionalProperties": true },
        "data": { "type": "object", "additionalProperties": true }
      },
      "description": "Goes through host.home.call_service; security domains are never covered by a wildcard target (4.2)."
    },
    "action_step": {
      "type": "object",
      "additionalProperties": false,
      "required": ["op", "kind"],
      "properties": {
        "op": { "const": "action" },
        "kind": { "type": "string" },
        "payload": { "type": "object", "additionalProperties": true }
      },
      "description": "Emits an action via host.action.emit; kind must match the recipe's declared permissions (actions:<kind>)."
    },
    "remember_step": {
      "type": "object",
      "additionalProperties": false,
      "required": ["op", "text"],
      "properties": {
        "op": { "const": "remember" },
        "text": { "type": "string" },
        "category": {
          "type": "string",
          "enum": [
            "person", "place", "thing", "preference", "identity", "event",
            "project", "goal", "relationship", "fact", "state"
          ]
        },
        "scope": { "type": "string", "enum": ["household", "person", "self"] }
      },
      "description": "Calls host.memory.remember."
    },
    "recall_step": {
      "type": "object",
      "additionalProperties": false,
      "required": ["op", "as", "query"],
      "properties": {
        "op": { "const": "recall" },
        "as": { "type": "string" },
        "query": { "type": "string", "description": "May reference input/variable names in {braces}." },
        "scope": { "type": "string", "enum": ["household", "person", "self"] },
        "limit": { "type": "integer", "minimum": 1, "maximum": 10, "default": 3 }
      },
      "description": "Calls host.memory.recall and binds a ready-to-speak summary of the top matches into the recipe's variable scope - a plain 'nothing found' phrase if there are none, since the recipe language has no conditional step to branch on that itself."
    },
    "schedule_step": {
      "type": "object",
      "additionalProperties": false,
      "required": ["op", "when"],
      "properties": {
        "op": { "const": "schedule" },
        "when": { "type": "string", "description": "A one-shot ISO time or a recurrence expression." },
        "job": { "type": "string", "description": "The job id this schedules, resolved by host.schedule." },
        "inputs": {
          "type": "object",
          "additionalProperties": true,
          "description": "Carried into the recipe's own input scope when the job fires and re-runs this package (session-d-packages-and-store.md step 8: host.schedule used to hardcode {} here, so a job scheduled from within a recipe re-fired with an empty scope - a known, documented gap, docs/dev.md's own scheduler entry). Every string value, at any depth, may reference input/variable names in {braces} - the same interpolate-before-send convention integration_call_step's own `args` already uses."
        }
      },
      "description": "Calls host.schedule."
    },
    "integration_call_step": {
      "type": "object",
      "additionalProperties": false,
      "required": ["op", "as", "id", "method"],
      "properties": {
        "op": { "const": "integration.call" },
        "as": { "type": "string", "description": "Binds the result into the recipe's variable scope under this name." },
        "id": { "type": "string", "description": "The integration id, e.g. \"home_assistant\". Requires the manifest to declare integration:<id>." },
        "method": { "type": "string", "description": "An id-specific method name, e.g. Home Assistant's \"get_state\" or \"call_service\"." },
        "args": { "type": "object", "additionalProperties": true, "description": "Every string value, at any depth, may reference input/variable names in {braces} - unlike fetch's own body, which is passed through as-is." }
      },
      "description": "Goes through host.integration.call - a typed read or action against a household-configured third-party integration beyond Home Assistant's own dedicated home.call_service step (session-d-packages-and-store.md step 4)."
    },
    "compute_step": {
      "type": "object",
      "additionalProperties": false,
      "required": ["op", "as", "expression"],
      "properties": {
        "op": { "const": "compute" },
        "as": { "type": "string" },
        "expression": {
          "type": "string",
          "description": "A math/unit-conversion expression, e.g. \"12 miles to km\" or \"{amount} * 1.08\". {variable} references are substituted before evaluation, the same interpolation every other step's text fields use."
        }
      },
      "description": "A restricted math/unit expression evaluator (session-d-packages-and-store.md step 4) - no network call, no host access, real numbers and unit conversion only. Backs math/convert without either package needing its own fetch-based service."
    },
    "llm_complete_step": {
      "type": "object",
      "additionalProperties": false,
      "required": ["op", "as", "prompt"],
      "properties": {
        "op": { "const": "llm_complete" },
        "as": { "type": "string", "description": "Binds the result ({\"text\": string}, the same raw-object shape fetch's own `as` binds) into the recipe's variable scope. Read the reply out with a `pick` step (`path: \"text\"`) before a `format` step interpolates it, the same two-step shape fetch+pick already uses." },
        "prompt": { "type": "string", "description": "A template with {variable} interpolation, sent as a single user-role message to the household's own local chat model. No system prompt, no conversation history: a one-shot completion for a lookup, not a chat turn." }
      },
      "description": "Goes through host.llm.complete (permission llm:complete) - the household's own local chat model (session-d-packages-and-store.md step 7's own translate package is the first caller). A network translation service is explicitly opt-in per the platform plan and not this step's concern: a recipe that wants one calls it through its own fetch step instead."
    },
    "list_add_step": {
      "type": "object",
      "additionalProperties": false,
      "required": ["op", "text"],
      "properties": {
        "op": { "const": "list_add" },
        "text": { "type": "string", "description": "May reference input/variable names in {braces}. The item text added to the household's own default shopping list." }
      },
      "description": "Calls host.lists.add (permission lists:write, session-d-packages-and-store.md step 8's own list-add package) - fire-and-forget, the same shape remember_step already takes: nothing is bound, a recipe's own format step confirms using the input it already has."
    },
    "list_view_step": {
      "type": "object",
      "additionalProperties": false,
      "required": ["op", "as"],
      "properties": {
        "op": { "const": "list_view" },
        "as": { "type": "string", "description": "Binds a ready-to-speak summary of the household's own default shopping list - a plain 'your list is empty' phrase if there are none, the same 'resolve a list-shaped result into one string at the interpreter, since the recipe language has no loop' move recall_step already makes." }
      },
      "description": "Calls host.lists.view (permission lists:read, session-d-packages-and-store.md step 8's own list-view package)."
    },
    "remind_step": {
      "type": "object",
      "additionalProperties": false,
      "required": ["op", "as", "text"],
      "properties": {
        "op": { "const": "remind" },
        "as": { "type": "string", "description": "Binds the result ({\"task\": string, \"when_text\": string}, the same raw-object shape fetch's own `as` binds) - a `pick` step reads each field out before a `format` step interpolates it." },
        "text": { "type": "string", "description": "May reference input/variable names in {braces}. The household member's own free-typed reminder request, e.g. \"at 6 to call Nadia\" - host.reminders.set does the natural-language time/task extraction and the real scheduling, both host-side, since neither is something this declarative step can do for itself." }
      },
      "description": "Calls host.reminders.set (permission reminders:write, session-d-packages-and-store.md step 8's own remind package) - schedules a core-kind job (session-d-packages-and-store.md step 8's own scheduler entry), never a replay of this recipe: firing later re-runs nothing, it raises the declared remind.due notification directly."
    },
    "timer_step": {
      "type": "object",
      "additionalProperties": false,
      "required": ["op", "as", "text"],
      "properties": {
        "op": { "const": "timer" },
        "as": { "type": "string", "description": "Binds the result ({\"label\": string, \"when_text\": string}, the same raw-object shape fetch's own `as` binds) - a `pick` step reads each field out before a `format` step interpolates it." },
        "text": { "type": "string", "description": "May reference input/variable names in {braces}. The household member's own free-typed timer request, e.g. \"for ten minutes\". host.timers.set parses the duration with a small deterministic parser, not a language model - a timer's whole point is exact minute-level accuracy, which natural-language date grammars and models are not reliably good at (session-d-packages-and-store.md step 8's own scheduler entry)." }
      },
      "description": "Calls host.timers.set (permission timers:write, session-d-packages-and-store.md step 8's own timer package) - schedules a core-kind job the same way remind_step does; firing later raises the declared timer.done notification directly, never a recipe replay."
    },
    "ask_step": {
      "type": "object",
      "additionalProperties": false,
      "required": ["op", "prompt"],
      "properties": {
        "op": { "const": "ask" },
        "prompt": { "type": "string", "description": "A template with {variable} interpolation, spoken/shown as the follow-up question." },
        "expects": { "type": "string", "description": "What kind of answer the next utterance should satisfy, e.g. a variable name the recipe would otherwise have asked for. Free text, matched by the turn engine (wave-2.md's C-to-D contract), not this interpreter." }
      },
      "description": "Sets the result's ask field (result.schema.json, 4.5) so a recipe that can't disambiguate on its own (\"which Springfield\") can ask a deterministic follow-up instead of guessing or failing outright. Always the recipe's last step: nothing after an ask step can run in the same pass, since there is nothing left to compute until the follow-up answer arrives on a later turn."
    }
  }
}
